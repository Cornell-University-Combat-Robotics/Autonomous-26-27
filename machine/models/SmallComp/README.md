This yolo26s model was used for February competition in the standard 640x640 image size. It was run on TensorRT on Matt's laptop, if you want closest results to comp, use this. I don't reccomend trying to lower the image size, as is it runs fast enough on the matt laptop. Theoretically, lowering the image size shouldn't decrease quality too heavily, but we have enough compute to where it isn't necessary.